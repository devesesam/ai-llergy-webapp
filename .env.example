# The shared Google Sheet (one tab per venue + substitutions tabs). Live id: see the sheet URL or
# Netlify -> Site configuration -> Environment variables. Verify with the set-menu builder's /api/health.
GOOGLE_SHEET_ID=

# Anthropic key for the custom-allergy interpreter (/api/interpret + AI-assisted filtering).
ANTHROPIC_API_KEY=
